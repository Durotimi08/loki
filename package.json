{
  "name": "loki",
  "private": true,
  "version": "0.0.0",
  "description": "Schema-agnostic, plug-in transaction tracking and bookkeeping library — Prisma for money-movement records.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "LOKI_INTEGRATION=1 vitest run",
    "test:all": "LOKI_INTEGRATION=1 vitest run",
    "test:types": "vitest --typecheck.only run",
    "typecheck": "pnpm -r --filter \"./packages/*\" typecheck",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "changeset": "changeset",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.27.9",
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.6"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild"]
  }
}
