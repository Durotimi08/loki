#!/usr/bin/env node
// Placeholder — the CLI surface lands with M1 codegen and migrations.
// Subcommands planned: `loki schema diff`, `loki schema migrate`,
// `loki generate`, `loki reconcile`, `loki tenant create|delete|relocate`.

export {}
